# mern-stack-register-login

After cloning the app,
Download and run dependent packages with;
npm install  and npm start for frontend,
npm install and npm run server for backend.
You can run the application.
